const sum = (a, b) => a + b

const subtract = (a, b) => a - b

const multiply = (a, b) => a * b

const divide = (a, b) => a / b

const modulo = (a, b) => a % b

const exponent = function(a, b) {
  return a ** b
}

module.exports = { sum, subtract, multiply, divide, modulo, exponent }
